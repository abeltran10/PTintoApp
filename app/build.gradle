apply plugin: 'com.android.application'

android {
    namespace 'com.abeltran10.ptinto'
    compileSdk 34 // Actualizado a una versión moderna

    defaultConfig {
        applicationId "com.abeltran10.ptinto"
        minSdk 21 // Soporte para el 99% de los dispositivos actuales
        targetSdk 34
        versionCode 1
        versionName "1.0"
        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    compileOptions {
        // Habilitar Java 8+ (Lambdas, etc.)
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    buildFeatures {
        viewBinding true // Recomendado sobre findViewById
    }
}

dependencies {
    // Librerías de UI modernas
    implementation 'androidx.appcompat:appcompat:1.6.1'
    implementation 'com.google.android.material:material:1.11.0'
    implementation 'androidx.constraintlayout:constraintlayout:2.1.4'

    // Testing moderno
    testImplementation 'junit:junit:4.13.2'
    androidTestImplementation 'androidx.test.ext:junit:1.1.5'
    androidTestImplementation 'androidx.test.espresso:espresso-core:3.5.1'
}